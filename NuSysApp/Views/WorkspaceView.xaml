<Page
    x:Class="NuSysApp.WorkspaceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" PointerPressed="Page_PointerPressed" DoubleTapped="Page_DoubleTapped"
    ManipulationDelta="Page_ManipulationDelta" ManipulationStarting="Page_ManipulationStarting"
    ManipulationInertiaStarting="Page_ManipulationInertiaStarting" ManipulationCompleted="Page_ManipulationCompleted" PointerWheelChanged="Page_PointerWheelChanged" RightTapped="Page_RightTapped" >

    <Page.Resources>
        <Style TargetType="Button" x:Key="FloatingButton" >
            <Setter Property="Template" >
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid Width="100" Height="100" Opacity=".75" ManipulationDelta="FM_ManipulationDelta" ManipulationStarting="FM_ManipulationStarting" ManipulationMode="All" RenderTransform="{Binding FMTransform}">
                            <Ellipse Height="85" Width="85" Fill="{TemplateBinding Background}" Opacity=".75"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid x:Name="maingrid" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" ManipulationMode="All">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="0*" />
        </Grid.RowDefinitions>
        <StackPanel x:Name="FM" HorizontalAlignment="Left" Visibility="Collapsed" >
            <Button Style="{StaticResource FloatingButton}" Click="AppBarButton_Click" HorizontalAlignment="Center" Content="global ink" Foreground="White">
                <Button.Background>
                    <ImageBrush ImageSource="global ink icon.png" Stretch="Uniform"></ImageBrush>
                </Button.Background>
            </Button>
            <Button Style="{StaticResource FloatingButton}"  HorizontalAlignment="Center">
                <Button.Background>
                    <ImageBrush ImageSource="link icon.png" Stretch="Uniform"></ImageBrush>
                </Button.Background>
            </Button>
            <Button Style="{StaticResource FloatingButton}" Click="AppBarButton_Click_Text" HorizontalAlignment="Center">
                <Button.Background>
                    <ImageBrush ImageSource="text icon.png" Stretch="Uniform"></ImageBrush>
                </Button.Background>
            </Button>
            <Button Style="{StaticResource FloatingButton}" Click="AppBarButton_Click_Scribble" HorizontalAlignment="Center">
                <Button.Background>
                    <ImageBrush ImageSource="scribble icon (1).png" Stretch="Uniform"></ImageBrush>
                </Button.Background>
            </Button>
            <Button Style="{StaticResource FloatingButton}" Click="AddButtonClick" HorizontalAlignment="Center">
                <Button.Background>
                    <ImageBrush ImageSource="image icon.png" Stretch="Uniform"></ImageBrush>
                </Button.Background>
            </Button>
        </StackPanel>
        <!--<Grid x:Name="lefttoolbar" HorizontalAlignment="Left" Margin="0,0,0,0" Width="50">
            <StackPanel x:Name="stackpanel" Background="#FF15C3F2" Opacity="0.5" Width="50" HorizontalAlignment="Left"></StackPanel>
            <Rectangle Fill="#FF15C3F2" HorizontalAlignment="Right" Margin="0,278,-10,278" Width="5" Opacity="0.5" 
                       RadiusY="5.25" RadiusX="5.25" />
            <Grid.Transitions>
                <TransitionCollection>
                    <EdgeUIThemeTransition Edge="Left"></EdgeUIThemeTransition>
                </TransitionCollection>
            </Grid.Transitions>
        </Grid>-->
        <ItemsControl Name="IC" ItemsSource="{Binding AtomViewList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas HorizontalAlignment="Left" Background="Beige" RenderTransform="{Binding CompositeTransform}"  x:Name="canvas">
                    </Canvas>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <InkCanvas IsRightTapEnabled="True" RightTapped="inkCanvas_RightTapped" Name="inkCanvas" RenderTransform="{Binding CompositeTransform}">
        </InkCanvas>
        
    </Grid>
</Page>