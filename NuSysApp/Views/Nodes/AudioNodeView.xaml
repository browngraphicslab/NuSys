<UserControl
    x:Class="NuSysApp.AudioNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="120"
    Width="480"
    RenderTransform ="{Binding Transform}"
    IsDoubleTapEnabled="True">
    <Grid Background="CornflowerBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" FontSize="18" HorizontalAlignment="Left" VerticalAlignment="Center"
                       Margin="20,0,0,0">FileName:</TextBlock>
            <TextBox x:Name="nodeTitle" Grid.Column="1" FontSize="18" HorizontalAlignment="Left"
                     VerticalAlignment="Center" Text="{Binding FileName, Mode=TwoWay}"></TextBox>
        </Grid>
        
        <!--<TextBox x:Name="nodeTitle" Grid.Column="0" Grid.Row="0" FontSize="24" Grid.ColumnSpan="2" Padding="5"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding FileName, Mode=TwoWay}"></TextBox>-->
        <Button x:Name="recordButton" Grid.Column="0" Grid.Row="1" Tapped="RecordButton_Click" FontSize="22"
                Width="200" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">Record</Button>
        <Button x:Name="playbackButton" Grid.Column="1" Grid.Row="1" Tapped="PlaybackButton_Click" FontSize="22"
                Width="200" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center">Play/Stop</Button>
        <TextBlock x:Name="recordingStatus" Grid.Column="0" Grid.Row="2" FontSize="16" HorizontalAlignment="Center"
                   VerticalAlignment="Top"></TextBlock>
        <TextBlock x:Name="playbackStatus" Grid.Column="1" Grid.Row="2" FontSize="16" HorizontalAlignment="Center"
                   VerticalAlignment="Top"></TextBlock>
        <MediaElement Grid.Row="0" Grid.Column="0" x:Name="playbackElement" Visibility="Collapsed"/>
    </Grid>
</UserControl>
