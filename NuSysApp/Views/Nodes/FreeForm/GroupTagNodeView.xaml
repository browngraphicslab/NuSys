<UserControl
    x:Class="NuSysApp.GroupTagNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:NuSysApp"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="{Binding Width}"
    Height="{Binding Height}"
    RenderTransform="{Binding Transform}"
>


    <local:NodeTemplate x:Name="nodeTpl" Background="LightGray" >
        <local:NodeTemplate.SubMenu>
            <StackPanel Orientation="Horizontal">
            </StackPanel>
        </local:NodeTemplate.SubMenu>
        <local:NodeTemplate.Inner>
            <Grid>
                <Border BorderBrush="{x:Null}" Height="50">
                    <TextBlock x:Name="Title" FontSize="40" Text="{Binding Title}" TextAlignment="Center" VerticalAlignment="Center"></TextBlock>
                </Border>
                
            </Grid>
        </local:NodeTemplate.Inner>
    </local:NodeTemplate>

</UserControl>
