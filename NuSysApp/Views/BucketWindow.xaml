<UserControl
    x:Class="NuSysApp.BucketWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border x:Name="Border">
        <Grid>
            <TextBox Height="10" Width="50" Margin="0,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="0" Background="{x:Null}" BorderBrush="{x:Null}" Text="IMPORTS"></TextBox>
            <SearchBox Width="100" Height="15" VerticalAlignment="Top" Margin="0,5,5,0" HorizontalAlignment="Right" BorderThickness="0"></SearchBox>
            <Grid x:Name="ContentContainer" Height="270" Width="390" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,5" Background="White" Opacity=".5">
                <ItemsControl></ItemsControl>
            </Grid>
            

        </Grid>
    </Border>
</UserControl>
