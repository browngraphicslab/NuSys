<UserControl
    x:Class="NuSysApp.BucketWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="600"
    d:DesignWidth="400">

    <Border x:Name="Border">
        <Grid>
            <Grid x:Name="ContentContainer" Height="550" Width="390" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5,35,5,5" Background="White" Opacity=".5">
                <ItemsControl></ItemsControl>
            </Grid>
            <TextBox Height="15" Width="80" Margin="5,5,5,5" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="0" Background="{x:Null}" BorderBrush="{x:Null}" Text="IMPORTS" Foreground="White" FontWeight="Bold"></TextBox>
            <SearchBox Width="100" Height="15" VerticalAlignment="Top" Margin="5,10,5,5" HorizontalAlignment="Right" BorderThickness="0"></SearchBox>

        </Grid>
    </Border>
</UserControl>
