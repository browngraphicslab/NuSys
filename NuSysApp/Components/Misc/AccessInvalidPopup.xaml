<UserControl
    x:Class="NuSysApp.AccessInvalidPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel x:Name="xAccessInvalidPopup" Height="200"  Width="300" Background="White" BorderThickness="2" BorderBrush="{StaticResource color2}">
        <TextBlock Margin="0,25,0,0"  Foreground="{StaticResource color3}" Width="250" TextWrapping="WrapWholeWords" TextAlignment="Center">
                Oops!
                You cannot put a private element on a public collection.</TextBlock>
        <Button Style="{StaticResource DarkButton}" Width="100" HorizontalAlignment="Center" Margin="0,20,0,0" Click="OK_OnClick">OK</Button>
        <Button Style="{StaticResource DarkButton}" Width="100" HorizontalAlignment="Center" Margin="0,20,0,0" Click="MakePublic_OnClick">Make Public</Button>
    </StackPanel>
</UserControl>
