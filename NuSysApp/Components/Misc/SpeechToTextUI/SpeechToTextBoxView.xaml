<UserControl
    x:Class="NuSysApp.Components.Misc.SpeechToTextUI.SpeechToTextBoxView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp.Components.Misc.SpeechToTextUI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="RootGrid"
          MinHeight="300" MinWidth="300"
          CanDrag="True"
          ManipulationDelta="RootGrid_OnManipulationDelta" ManipulationMode="TranslateX, TranslateY, TranslateInertia">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RenderTransform>
            <TransformGroup x:Name="xTransformGroup">
                <CompositeTransform x:Name="xCompositeTransform"/>
                <MatrixTransform x:Name="xMatrixTransform"/>
            </TransformGroup>
        </Grid.RenderTransform>
        <Button x:Name="MicButton" Content="&#xE720;" FontFamily="Segoe MDL2 Assets" Background="White"></Button>
        <TextBox x:Name="xTextBox" Text="{x:Bind _vm.Text, Mode=TwoWay}" Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White"></TextBox>
    </Grid>
</UserControl>

