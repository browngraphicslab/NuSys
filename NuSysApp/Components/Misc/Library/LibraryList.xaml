<UserControl
    x:Class="NuSysApp.LibraryList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>

        <ListView  x:Name="ListView"   >
            <ListView.Header>
                <Grid VerticalAlignment="Center" Background="White ">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="125" />
                        <ColumnDefinition Width="125" />
                    </Grid.ColumnDefinitions>


                    <TextBlock HorizontalAlignment="Center" Grid.Column="0"  Text="TITLE" FontSize="18" FontFamily="FiraSans-Book"/>
                    <TextBlock HorizontalAlignment="Center" Grid.Column="1" Text="TYPE" FontSize="18" FontFamily="FiraSans-Book"/>
                    <TextBlock HorizontalAlignment="Center" Grid.Column="2" Text="DATE" FontSize="18" FontFamily="FiraSans-Book"/>
                    

                </Grid>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid x:Name="LibraryListItem"  VerticalAlignment="Center" Padding="0" Margin="0" Background="#ECECEA" ManipulationMode="TranslateX, TranslateY" PointerPressed="LibraryListItem_OnPointerPressed" DoubleTapped="ListItem_OnDoubleTapped" IsDoubleTapEnabled="True" ManipulationStarted="LibraryListItem_ManipulationStarted"  ManipulationDelta="LibraryListItem_ManipulationDelta" ManipulationCompleted="LibraryListItem_ManipulationCompleted">
                            <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="125" />
                </Grid.ColumnDefinitions>
                        
                      
                        <TextBlock  Grid.Column="0"  Text="{Binding Title}" FontSize="16" />
                        <TextBlock  Grid.Column="1"  Text="{Binding Type}" FontSize="16"/>
                        <TextBlock Grid.Column="2" Text="{Binding Timestamp}" FontSize="16"/>
                     

                    </Grid>
                    </DataTemplate>

            </ListView.ItemTemplate>
            
        </ListView>
       
    </Grid>
</UserControl>
