<UserControl
    x:Class="NuSysApp.LibraryList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid BorderBrush="#83A6A3" BorderThickness="1">

        <ListView  x:Name="ListView" ItemClick="ListView_OnItemClick" IsItemClickEnabled="True">
            <ListView.Header>
                <Grid VerticalAlignment="Center" Background="White ">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="125" />
                        <ColumnDefinition Width="125" />
                    </Grid.ColumnDefinitions>


                    <Button HorizontalAlignment="Center" Grid.Column="0" Click="Sort_Button_Click" Foreground="#9cc5c2"  FontSize="18" FontFamily="ms-appx:/Assets/Orator-Std-Medium_34151.ttf#Orator Std">
                        title
                    </Button>
                    <Button HorizontalAlignment="Center" Grid.Column="1" Click="Sort_Button_Click" Foreground="#9cc5c2"   FontSize="18" FontFamily="ms-appx:/Assets/Orator-Std-Medium_34151.ttf#Orator Std">
                    type
                    </Button>
                    <Button HorizontalAlignment="Center" Grid.Column="2" Click="Sort_Button_Click" Foreground="#9cc5c2"  FontSize="18" FontFamily="ms-appx:/Assets/Orator-Std-Medium_34151.ttf#Orator Std">
                   date
                    </Button>


                </Grid>
            </ListView.Header>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid x:Name="LibraryListItem"  VerticalAlignment="Center" Padding="0" Margin="0" BorderBrush="Red" BorderThickness="{Binding BorderThickness}" Background="#ECECEA" ManipulationMode="TranslateX, TranslateY" PointerPressed="LibraryListItem_OnPointerPressed" DoubleTapped="ListItem_OnDoubleTapped" IsDoubleTapEnabled="True" ManipulationStarted="LibraryListItem_ManipulationStarted"  ManipulationDelta="LibraryListItem_ManipulationDelta" ManipulationCompleted="LibraryListItem_ManipulationCompleted">
                            <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="125" />
                    <ColumnDefinition Width="125" />
                </Grid.ColumnDefinitions>
                        
                      
                        <TextBlock  Grid.Column="0" Text="{Binding Title}" FontSize="16"/>
                        <TextBlock  Grid.Column="1" Text="{Binding Type}" FontSize="16"/>
                        <TextBlock Grid.Column="2" Text="{Binding Timestamp}" FontSize="16"/>
                     

                    </Grid>
                    </DataTemplate>

            </ListView.ItemTemplate>
            
        </ListView>
       
    </Grid>
</UserControl>
