<local:AnimatableUserControl
    x:Class="NuSysApp.RecordingNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border x:Name="xRootBorder" BorderThickness="1" BorderBrush="Black" Width="{Binding Width}" Height="{Binding Height}" 
            ManipulationDelta="XRootBorder_OnManipulationDelta" ManipulationMode="TranslateX, TranslateY">
        <Border.RenderTransform>
            <CompositeTransform TranslateX="{Binding X}" TranslateY="{Binding Y}"/>
        </Border.RenderTransform>
        <local:MediaRecorderView x:Name="xMediaRecorder"  Width="{Binding Width}" Height="{Binding Height}"/>
    </Border>

</local:AnimatableUserControl>
