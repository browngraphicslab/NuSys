<local:AnimatableUserControl
    x:Class="NuSysApp.AreaNodeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Width="{Binding Width}"
    Height="{Binding Height}"
    HorizontalAlignment="Left" VerticalAlignment="Top"
>
    <Canvas x:Name="xCanvas" Width="{Binding Width}" Height="{Binding Height}" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top">
        <Canvas.Clip>
            <RectangleGeometry Rect="{Binding ClipRect}"></RectangleGeometry>
        </Canvas.Clip>
        <Canvas x:Name="xAtomContainer" HorizontalAlignment="Center" Width="100000" Height="100000" Background="Transparent"  RenderTransform="{Binding CompositeTransform}">
            <ItemsControl Name="IC" ItemsSource="{Binding AtomViewList}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas  />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Canvas>
    </Canvas>
</local:AnimatableUserControl>
