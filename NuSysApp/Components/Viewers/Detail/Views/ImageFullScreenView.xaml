<UserControl
    x:Class="NuSysApp.ImageFullScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
>
    <Grid x:Name="totalStackPanel" VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel x:Name="buttons" Orientation="Horizontal" Background="#565F6D" VerticalAlignment="Top">
            <Button Click="ButtonBase_OnClick" Foreground="White" Background="Transparent">INK</Button>
            <Button Name="SourceBttn" Foreground="White" Click="OnGoToSource" Background="Transparent">GO TO SOURCE</Button>
        </StackPanel>
        <Image x:Name="xImg" Source="{Binding Image}" Stretch="Uniform" VerticalAlignment="Top" Margin="0,30,0,0"/>
    </Grid>
</UserControl>
