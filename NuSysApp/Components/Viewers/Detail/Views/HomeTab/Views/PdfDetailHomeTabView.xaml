     <UserControl
    x:Class="NuSysApp.PdfDetailHomeTabView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ms-appx:///ResourceDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel  VerticalAlignment="Top" Orientation="Horizontal" Background="#565F6D" Visibility="Collapsed">
            <Button Name="SourceBttn" Foreground="{StaticResource foreground6}" Click="OnGoToSource">GO TO SOURCE</Button>
        </StackPanel>
        <StackPanel>
            <Border x:Name="xBorder"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid x:Name="xWrapper" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Image x:Name="xImg" PointerPressed="xImg_PointerPressed" Source="{Binding ImageSource}" Stretch="Uniform">
                        <Image.RenderTransform>
                            <CompositeTransform></CompositeTransform>
                        </Image.RenderTransform>
                    </Image>
                    <ItemsControl x:Name="ItemsControl"  ItemsSource="{Binding RegionViews}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                    <Button x:Name="pageLeft" Height ="50" Width="50" VerticalAlignment="Center" Background=" Transparent" HorizontalAlignment="Left" Click="OnPageLeftClick">
                        <Button.Content>
                            <Image Opacity=".8" Stretch="Uniform" Source="ms-appx:///Assets/icon_pdf_left.png" />
                        </Button.Content>
                    </Button>
                    <Button x:Name="pageRight" Height ="50" Width="50" VerticalAlignment="Center" Background="Transparent" HorizontalAlignment="Right" Click="OnPageRightClick">
                        <Button.Content>
                            <Image Opacity=".8" Stretch="Uniform" Source="ms-appx:///Assets/icon_pdf_right.png" />
                        </Button.Content>
                    </Button>
                </Grid>
            </Border>
            <Border Background="{StaticResource color7}" Height="Auto" Width="140" Padding="5" BorderBrush="{StaticResource color3}" BorderThickness="2" Margin="0,50,0,0"
                Visibility="{Binding Editable, Converter={StaticResource vConverter}, ConverterParameter=true}">
                <TextBlock x:Name="AddToCollection" Width="Auto" Height="Auto" Foreground="{StaticResource color3}" Text="Drag To Collection" TextAlignment="Center"
                   PointerPressed="AddToCollection_OnPointerPressed"
                   ManipulationStarted="AddToCollection_OnManipulationStarted"
                   ManipulationDelta="AddToCollection_ManipulationDelta"
                   ManipulationCompleted="AddToCollection_ManipulationCompleted"
                   ManipulationMode="TranslateX,TranslateY"/>
            </Border>
        </StackPanel>
        <StackPanel Visibility="Collapsed">
            <TextBlock Text="SUGGESTED TAGS" Foreground="{StaticResource foreground6}" FontSize="30" CharacterSpacing="45" Margin="10,15,30,0" Padding="5,0,5,0"></TextBlock>
            <Line Stroke="{StaticResource color6}" StrokeThickness="1.5" X1="0" Y1="0" X2 ="50" Y2="0" Margin="0,15,0,0"></Line>
            <ItemsControl x:Name="SuggestedTags" ItemsSource="{Binding SuggestedTags}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Grid>
    
</UserControl>
