<local:AnimatableNodeView
    x:Class="NuSysApp.TextDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Border x:Name="xBorder" BorderThickness="2" BorderBrush="White" Width="500" Height="500">
        <StackPanel x:Name="grid" Background="White" VerticalAlignment="Top" Margin="5,5" Width="500" Height="500">
            <StackPanel Orientation="Horizontal">
                <Button Tapped="OnRecordClick">Record</Button>
            </StackPanel>
            <local:TextBoxExtended x:Name="rtfTextBox" ScrollViewer.ZoomMode="Disabled" ScrollViewer.VerticalScrollMode="Disabled" Foreground="Black"  TextWrapping="Wrap" BorderThickness="0" FontSize="20" FontFamily="Calibri"
                                     BorderBrush="Transparent" AcceptsReturn="True" Visibility="Visible" VerticalAlignment="Stretch" />
        </StackPanel>
    </Border>
</local:AnimatableNodeView>
