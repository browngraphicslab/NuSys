<Page
    x:Class="NuSysApp.SessionView"
    x:Name="mainFrame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"  
    >

    <Canvas x:Name="mainCanvas" Background="Transparent" AllowDrop="True" DragLeave="OnDrop">
        <local:FloatingMenuView x:Name="xFloatingMenu" />
        <local:MediaRecorderView x:Name="xMediaRecorder" VerticalAlignment="Top"/>
        <TextBox x:Name="xWorkspaceTitle" TextWrapping="NoWrap" AcceptsReturn="False" MaxHeight="50"  Text="Unnamed Workspace" FontSize="26" BorderBrush="Transparent" TextAlignment="Right" VerticalAlignment="Center"></TextBox>
        <Button x:Name="xRecord" Click="OnRecordClick" Height="47" VerticalAlignment="Center">
            <Image Source="/Assets/icon_record.png"></Image>
        </Button>
        <StackPanel Name="Users" Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Right" Width="200">
        </StackPanel>
        <local:FullScreenViewerView x:Name="xFullScreenViewer"></local:FullScreenViewerView>

        <Grid Name ="overlayCanvas" Background="#55000000" Tapped="SearchHide" Visibility ="Collapsed">
            <local:SearchWindowView x:Name="xSearchWindowView"></local:SearchWindowView>
        </Grid>
      



    </Canvas>
   

</Page>