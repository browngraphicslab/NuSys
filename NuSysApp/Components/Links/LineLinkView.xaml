<UserControl
    x:Class="NuSysApp.LineLinkView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Line Name="line" Stroke="#FF9C9C9C" StrokeThickness="2" X1="{Binding Atom1.AnchorX}"
              Y1="{Binding Atom1.AnchorY}" X2="{Binding Atom2.AnchorX}" Y2="{Binding Atom2.AnchorY}" />

    </Grid>
</UserControl>