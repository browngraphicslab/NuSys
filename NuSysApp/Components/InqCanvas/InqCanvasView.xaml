<UserControl
    x:Class="NuSysApp.InqCanvasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuSysApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="Parent">
    <!--<Canvas x:Name="xContainer">
        --><!--<ItemsControl ItemsSource="{Binding Lines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas HorizontalAlignment="Left" Background="Transparent" Width="{Binding ElementName=Parent, Path=Width}" Height="{Binding ElementName=Parent, Path=Height}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>--><!--
        <Image x:Name="xCanvas" Width="{Binding CanvasSize.Width}" Height="{Binding CanvasSize.Height}"/>
        --><!--<Image x:Name="xCanvas" Width="5000" Height="5000"/>--><!--
    </Canvas>-->
    <Grid x:Name="xGrid" Background="Transparent">
        <Image x:Name="xCanvas" Width="{Binding CanvasSize.Width}" Height="{Binding CanvasSize.Height}" Source="{Binding CanvasSource}"/>
    </Grid>
</UserControl>
